# Дата: 2026-02-17

## Что было сделано:

- Инициализировал проект на Vite + React.
- Настроил ESLint и Prettier с базовыми правилами (частично использовал конфиги из предыдущих проектов).
- Подключил Husky (pre-commit) и commitlint для стандартизации коммитов.
- Добавил скрипты
- Проверил, что хуки корректно отрабатывают при коммите.

## Проблемы:

1. Изначально создал проект через мастер Vite в отдельной вложенной папке.
   После этого Husky не отрабатывал корректно — были проблемы с привязкой к git root.
2. Также возникли сложности с PR template — GitHub не подхватывал шаблон из-за структуры проекта.

## Решения (или попытки):

После анализа и обсуждения с GPT принял решение:

- Перенести проект в корень репозитория.
- Переинициализировать Husky уже из root-директории.

После этого:

- pre-commit начал отрабатывать,
- commitlint валидирует сообщения,

## Мысли / Планы:

- Очистить проект от демо-кода Vite.
- Определится с темой проэкта который будет делать с командой.

## Затраченное время:

Примерно 3–4 часа (работал в несколько подходов, частично в перерывах на работе).
