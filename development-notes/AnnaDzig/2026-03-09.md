# Date: 2026-03-09

- **What was done:**

Today I continued implementing the Main Page.
I finished the **How It Works** section and improved the structure of how icons are handled. Instead of keeping icon imports directly inside the component, I moved the mapping to a separate configuration file. This made the component cleaner and easier to read.
After that I started working on the **Topics section**. Since this section contains several cards with similar layout, I created a reusable `TopicCard` component.

Each card now displays:

- topic image
- title
- short description
- level label
- button

The section then simply maps the topic data and renders the cards.
I also spent a bit more time checking how the images look inside the cards and adjusting spacing so everything feels balanced.

- **Decisions / Notes:**

Decided to:

- keep data separate from UI components
- move icon mappings into a separate config file
- extract reusable components when layout repetition appears

This keeps the page easier to maintain as it grows.

- **Problems / Questions:**

The main issue today was again related to TypeScript when mapping icons and images. Since the key was typed as a generic string, TypeScript did not allow indexing the mapping object.
The solution was to define stricter types for the IDs used in the data.

- **Plans / Next steps:**

- Finish the Topics section
- Implement the Mini Games section
- Connect all sections inside `MainPage.tsx`
- Polish spacing and visual details

- **Time spent:** ~4 hours
