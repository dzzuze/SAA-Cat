# Date: 2026-03-23

- **What was done:**

Today I focused on preparing the advanced JavaScript topics for our application. At first, I was working on structuring everything inside JSON with properly formatted Markdown. This required handling line breaks, code blocks, and escaping characters, which made the process quite fragile and time-consuming.
Later, we realized that for Firebase we actually need to store the `content` field directly in Markdown format, without escaping it inside JSON. This changed the approach significantly.

After that, I split the work into two parts. First, I prepared the content itself — making sure each topic is clear, well-structured, and readable. Then I started filling Firebase, adding topics one by one directly in Markdown format. I didn’t focus on counting topics, but rather on ensuring each one is correct and consistent.

I also fixed a Git issue related to the branch name, which prevented pushing changes. After renaming the branch, everything worked correctly.

In addition, I spent time preparing for tomorrow’s interview by reviewing key JavaScript concepts and general explanations.

---

- **Decisions / Notes:**

Decided to:

- store content directly in Markdown for Firebase instead of escaped JSON
- separate content preparation from data input (Firebase filling)
- focus on clarity and consistency rather than speed

This approach makes the content easier to maintain and avoids formatting issues.

---

- **Problems / Questions:**

The main issue was working with Markdown inside JSON. Escaping characters (`\\n`, quotes) made the content harder to manage and easy to break.

Also had a Git issue:

- branch name caused push errors
- resolved by renaming the branch

---

- **Plans / Next steps:**

- Continue filling Firebase with remaining topics
- Connect topics to UI rendering
- Improve content structure if needed
- Continue interview preparation

---

- **Time spent:** ~4–5 hours
